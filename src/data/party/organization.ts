import {Party} from "./party";

export class Organization extends Party {
  partyId: any;
  name: string;
  purpose: string;
  email: string;
  number: string;
  address: string;
  description: string;
}
